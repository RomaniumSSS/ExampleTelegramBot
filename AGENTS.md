# AGENTS.MD — Инструкция для AI-агентов

Этот документ является **единым источником правды** для любого AI-агента, работающего над проектом "Vibe Tracker". Он определяет рабочий процесс, правила и соглашения.

---

## 1. Базовые принципы (Core Principles)

1.  **Documentation-First Development** — *каждая* сессия начинается с чтения всех файлов в папке `docs/` (`product.md`, `tech.md`). Не начинай писать код, пока не поймешь текущую архитектуру и задачу.
2.  **Instruction-First** — Для простых задач следуй инструкциям человека напрямую. Для сложных задач — сначала планируй.
3.  **Keep It Simple (MVP)** — Мы делаем MVP. Избегай оверинжиниринга. Используй простые и понятные решения, соответствующие стеку (`aiogram`, `Tortoise ORM`, `SQLite`).
4.  **AICODE System** — Оставляй "хлебные крошки" в коде для будущих сессий (см. раздел 4).

---

## 2. Протокол выполнения задач

### Шаг 1: Погружение в контекст
В начале любой задачи выполни команду чтения документации:
```bash
cat docs/product.md docs/tech.md
```
Это обязательно, чтобы понимать контекст "Вайб Трекера" и не предлагать решения, противоречащие стеку (например, PostgreSQL вместо SQLite).

### Шаг 2: Оценка сложности
*   **Простая задача** (фикс бага, мелкая правка): Сразу приступай к реализации.
*   **Сложная задача** (новая фича, изменение схемы БД): Сначала предложи **План** (список шагов) и дождись подтверждения (👍 или "Ок").

### Шаг 3: Реализация и обновление документации
*   Пиши чистый код, соблюдая PEP8.
*   **ВАЖНО:** Если ты меняешь логику работы, добавляешь новые переменные окружения или меняешь схему БД — **сразу обнови** соответствующие файлы в `docs/` (`tech.md` или `product.md`) и `README.md`. Документация не должна устаревать.

---

## 3. Система памяти AICODE

Используй специальные комментарии-анкоры, чтобы передавать контекст следующему агенту или сессии. Это помогает не терять мысли между задачами.

Используй формат: `# AICODE-<ТИП>: <сообщение>`

*   `AICODE-NOTE:` — важное примечание о том, почему сделано именно так (например, "хак" для совместимости).
*   `AICODE-TODO:` — известная проблема или недоделка, которую надо решить в будущем, но не сейчас.
*   `AICODE-QUESTION:` — неопределенность, требующая внимания человека.

**Пример:**
```python
# AICODE-NOTE: Используем polling вместо webhook для простоты MVP
await dp.start_polling(bot)

# AICODE-TODO: Добавить middleware для логирования ошибок
```

---

## 4. Технические правила (Vibe-Coding Style)

1.  **Стек:** Python 3.11+, aiogram 3.x, Tortoise ORM, SQLite.
2.  **Асинхронность:** Весь I/O (база данных, запросы к Telegram) должен быть асинхронным (`async`/`await`).
3.  **Структура:** Строго следуй структуре папок, описанной в `docs/tech.md`. Не создавай файлы в корне, если им место в `handlers/` или `services/`.
4.  **Безопасность:** Токены только в `.env`. Никогда не хардкодь секреты.

---

## 5. Самопроверка перед завершением

Перед тем как сказать "Готово", проверь себя:
1.  [ ] Код соответствует задаче?
2.  [ ] Никаких лишних файлов не создано?
3.  [ ] Документация (`docs/`) обновлена, если что-то изменилось?
4.  [ ] Линтеры не нужны, но код должен выглядеть опрятно (PEP8).
